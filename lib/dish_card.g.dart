// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'dish_card.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class DishCard extends HookWidget {
  const DishCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _dishCard();
}

class _DishCardContent extends StatelessWidget {
  const _DishCardContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => __dishCardContent();
}

class _DishCardContentEnd extends StatelessWidget {
  const _DishCardContentEnd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => __dishCardContentEnd();
}

class _LikeRow extends StatelessWidget {
  const _LikeRow(this.icon, this.colorText, this.text, {Key? key})
      : super(key: key);

  final Icon icon;

  final Color colorText;

  final String text;

  @override
  Widget build(BuildContext _context) => __likeRow(icon, colorText, text);
}

class _LikeAndDislikeRow extends HookWidget {
  const _LikeAndDislikeRow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => __likeAndDislikeRow();
}
